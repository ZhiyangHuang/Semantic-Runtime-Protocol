# SRP Experiment Provenance

This directory preserves historical SRP experiment assets that are no longer part of the active release execution path.

Source location:

```text
srp_experiment/
```

Migration phase:

```text
Phase 6.5-B2
```

Reason:

- historical reproducibility
- provenance retention
- release boundary clarity

Execution status:

- not part of active release execution

Current intent:

- preserve historical experiment inputs, schemas, scratch traces, and legacy evidence helpers
- keep executable compatibility assets separate from provenance-only material
