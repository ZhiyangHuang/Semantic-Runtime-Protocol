# Reasoning Sources Provenance

This source family is registered as an external evaluation input for SRP boundary reporting.

Reasoning sources are used to generate candidate semantic transitions, not to
measure reasoning capability.

The repository does not store the benchmark payload.

Only source metadata, adapter mapping, and provenance notes are retained here.
