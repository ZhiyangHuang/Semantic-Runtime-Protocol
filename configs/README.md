# Configs

This directory keeps one shared root file plus a small set of scenario-specific overlays.

## Core File

- `root.env`

## Scenario Files

- `phase_viii_representation_invariance.env`
- `phase_viii_implementation_independence.env`
- `external_validation_locomo_manual_sanity.env`
- `external_validation_locomo_mvp_calibration_aware.env`
- `external_validation_longmemeval_evidence.env`
- `external_validation_longmemeval_reality_check.env`

`root.env` is the default local runtime entry point and now carries the shared model defaults.
