# Phase II closure validation configuration.
# This file keeps validation inputs separate from the legacy root .env.

SRP_PHASE=phase_ii_validation
VALIDATION_MODE=closure_validation
VALIDATION_DIMENSIONS=boundary_stability,cross_condition_robustness,reproducibility,evidence_consistency
VALIDATION_BACKEND=vector
GOVERNANCE_REQUIRED=true
RUNTIME_MUTATION_ALLOWED=false
