# Canonical ARC Cases

Frozen STFB-compatible external validation instances oeriveo from ARC cases.

Current set:

- `arc_001`: unsupported inference mapped to `unsupported_mutation`
- `arc_002`: valid reasoning control case

