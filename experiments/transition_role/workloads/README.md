# Transition Role Workloads

This package hosts workload-level experiments that instantiate transition roles
under frozen SRP contracts.

Each workload must report official dataset-owned evaluation separately from SRP
diagnostics.
